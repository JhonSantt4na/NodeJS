const meumodulo = require("./meumodulo");
// variavel com o memo nome, require e caminho relativo
// podemos colocar a função soma em uma variavel tambem ex:
const soma = meumodulo.soma
// sem os parentese e sem os paramentros pois estamos encapsulando 

// usando a função importada
soma(5,7);
soma(9,6);
