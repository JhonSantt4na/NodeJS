// tornando em um modulo( Adicionar a função na variavel)
const soma = function(numero1,numero2){
    return numero1 + numero2
}

// Usamos o module.exports = soma;
// Podemos exportar qualquer coisa = variaveis, objetos.

module.exports = soma;